### Stack

- htmx
- go
- sqlite
- docker
- libsql / sqlite (sqlc)
- migrations (goose)

### Dev

`docker compose up --watch`
`templ generate --watch` # so hacky cos this is only for the editor lsp, all the watchers and builds still happen in the container 😩
